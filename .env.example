# Copy to `.env` for local development (never commit `.env`).
# Google AI Studio: https://aistudio.google.com/app/apikey

GEMINI_API_KEY=
# Optional: override default model (see Google Generative AI docs)
# GEMINI_MODEL=gemini-2.0-flash
